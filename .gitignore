# Terraform working files and local state
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log

# Local variable values and overrides (examples may be committed)
*.tfvars
*.tfvars.json
!*.tfvars.example
!*.tfvars.json.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Local credentials, environment files, and private keys
.env
.env.*
!.env.example
.aws/
.kube/
.docker/
credentials
credentials.json
kubeconfig
kubeconfig.*
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519

# Local logs and OS files
*.log
.DS_Store

# Keep .terraform.lock.hcl in Git for reproducible provider selection.
